{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@daemon-protocol/kernel",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "Deterministic shielded kernel for the GATE_3 remediation gate (Daemon Protocol)",
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "typescript": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
